# Seeder - MODELS

```bash
npx sequelize-cli seed:generate --name demo-area
npx sequelize-cli seed:generate --name demo-rol
npx sequelize-cli seed:generate --name demo-recurso
npx sequelize-cli seed:generate --name demo-usuario
npx sequelize-cli seed:generate --name demo-categoria
npx sequelize-cli seed:generate --name demo-proveedor
npx sequelize-cli seed:generate --name demo-producto
npx sequelize-cli seed:generate --name demo-fotografia
npx sequelize-cli seed:generate --name demo-opinion
npx sequelize-cli seed:generate --name demo-compra
```